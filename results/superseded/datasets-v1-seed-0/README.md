# Superseded development results

These 15 nonlearned runs used the earlier `datasets-v1` condition universe and
baseline seed 0. They were retained for audit only after the benchmark contract
changed to `datasets-v2`, the frozen GEARS default-graph representability
intersection, and shared seed 1.

Do not add these paths to a `ResultCatalog` or use their metrics in the current
benchmark. Current replacements must be regenerated from the five
`datasets-v2` split/control manifests and written under a new run namespace.
