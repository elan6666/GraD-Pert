# Plan 036 — TxPert candidate graph-axis H4

## Objective

Add one direct-to-A0 H-axis endpoint that tests the frozen public TxPert
candidate-gene universe without changing the Nadig expression output axis or
any other A0 scientific factor.

## Frozen coordinate

- Variant: `h4_txpert_candidate_ratio_half`.
- Global Teacher/Student graph axis: the exact ordered 9,853 symbols in public
  TxPert `gears_gene_set.csv` at commit
  `08d82eea86746b044cf7531f4ec8c5f60e1cb73f`, file SHA-256
  `7e2be69a204b72349b793cc6723a5f88419f1ca6472ea5e28c5f7d623ee8e23d`.
- No control graph node. Every canonical Nadig perturbation target must exist.
- Four RingInduced locals, ratio `1/2`, resolved by floor to 4,926 nodes.
- STRING and GO independently Top-20 incoming; learned ID; additive decoder;
  all remaining data, split, training, loss and evaluation settings equal A0.

## Execution barrier

Do not touch the active L checkout or queues. After L1--L5 complete and pass
strict delivery, publish one new clean local/GitHub/server commit, materialize
and seal the H4 graph, run a one-step real-data CUDA capacity gate, then run one
formal 10-epoch H4 row. M remains behind H4.

## Acceptance

- Matrix/config and source-file identities fail closed under drift.
- Graph receipt proves exact candidate order/count, target coverage, source
  graph hashes, topology hash, no control node and 4,926-node local resolution.
- Local and server pytest, Ruff, format, strict mypy and isolated build pass.
- Capacity passes before the formal row.
- Formal evidence has 5,820 ordered steps, ten validations, one best-checkpoint
  test evaluation, exact three metrics, zero persistent PKL and only `best.pt`.
