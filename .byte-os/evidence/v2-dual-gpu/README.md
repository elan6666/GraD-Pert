# Synchronous dual-GPU engineering evidence

These source-bound receipts are separate from historical single-GPU capacity.
`micro32-eval128-7c17d3a.json` passed128 updates with world2, accumulation1,
physical32/rank and global64; midpoint reload and300-control/5000-gene
inference passed. It is an engineering probe, not50-epoch scientific evidence.

The parameter census sums original student optimizer route shapes from the
passed74/rank repeat:43,808,513 total, including20,980,736 in four distillation
heads. The remaining22,827,777 include the base model, masks and CLS tokens;
this is not a claim that every parameter is exercised in inference. EMA copies
and buffers are excluded.

Original source SHAs remain authoritative. `git diff` confirms no changes to
`src/`, capacity_probe.py or inference_probe.py between afbed5a and858583d.
The64/rank full probe is still pending at this revision; do not infer its result
from the successful single-update check or from single-GPU evidence.
