# Four-local A/H formal review

## Verdict

Pass for the four scientific rows at source `845c10a`. A0, H1, H2 and H3 each
completed the preregistered ten-epoch protocol and satisfy the formal
zero-PKL/best-checkpoint contract. L remains unrun and unauthorized.

## Evidence checked

- Recomputed 5,820 contiguous global steps and ten ordered validations per row.
- Confirmed test truth was absent during fitting and exactly one evaluation
  used the hash-matched retained `best.pt`.
- Confirmed the exact three finite headline metrics and identical canonical
  data, split, condition order, 300-control rows and truth-row identities.
- Confirmed every row uses four RingInduced locals, ratio `1/2`, mask ratio
  `0/1`, Exphormer-MG STRING+GO and the CPU-vectorized sparse union.
- Confirmed zero PKL over the full lineage, no evaluation work directory and
  exactly one `best.pt` per row.
- Rehashed both terminal queue receipts and every row receipt. The compact
  aggregate is `../evidence/vnext-performance/formal-ah-four-local-845c10a.json`.

## Scientific reading

The results are single-seed descriptive direct comparisons to A0. H1 is
nearly identical to A0 on the TxPert macro point estimate but lower on the
TriShift and Systema point estimates. H2 is lower on all three point estimates.
H3 is lower on the two delta metrics and higher on Systema Pearson. This mixed
pattern does not justify changing the default HVG512 axis, and no equivalence
or general superiority claim is made.

## Trackio boundary

Trackio remained non-authoritative. A0 and H3 recorded complete owner-only
local stores, but the subsequent private Bucket archive failed. H1 and H2
could not start their local client because the long-lived reverse SOCKS
endpoint refused the live Bucket preflight connection. Native scientific
receipts were unaffected. No tracking replay or lineage mutation was attempted.
